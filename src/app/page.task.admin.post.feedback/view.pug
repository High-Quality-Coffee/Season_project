.header
    h1 Feedback
    nav
        ul
            li
                a(routerLink="/task/admin/notice") 뒤로가기
    p {{list.user_name}} 지원자를 대상으로 한 피드백 입니다.
    
.card-body
    .info-form.row.first-child.p-3
        .col-md-8
            h3.m-0(style="font-size: 20px; line-height: 24px;") 제목 : {{ list.title }}
        .col-md-3.text-right
            .btn.btn-sm.btn-outline-orange 작성자 
            span(style="margin-left: 10px;") {{list.writer}}
    
    .info-form.row.ml-3(*ngIf="post.files && post.files.length > 0")
        .col-md-1
            h4 첨부파일
        .col-md-11
            ul.m-0
                li(*ngFor="let file of post.files;let i = index")
                    a(href="/file/download/{{post.category}}/{{post.id}}/{{file}}/{{file}}?attatch=false" target="_blank") {{file}}
    .hr.m-0
    .editor-area.mt-4.mb-4
        textarea#editor {{list.content}}
    .info-form.row
            .col-md-8
                form#file
                    input#file-upload.form-control(type='file', name="file", multiple, (change)='upload($event)')
    .info-form.row(*ngIf="post.files && post.files.length > 0")
        .col-md-2
        .col-md-10
            ul.m-0
                li(*ngFor="let file of post.files;let i = index")
                    span {{file}}
                    button.btn.btn-sm.btn-square.btn-danger.ml-1(style="padding: 0 4px;", (click)="delete_file(file)") X

