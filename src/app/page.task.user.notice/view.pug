.header
    h1 과제 목록
    nav
        ul
            li
                a((click)="movetoNext()") 내 정보
            li
                a((click)="logout()") 로그아웃
    p 해결해야 할 과제입니다.

#modal-score
    .modal-content 로그아웃
        p(style="color:black;") 정말로 로그아웃 하시겠습니까?
        .modal-btn
            button.modal-close((click)="logout('close')") 창닫기
            button.modal-save((click)="logout('logout')") 로그아웃
    .modal-layer

.card.border-0.table-card.round-0.shadow-none.px-0
            .table-responsive.round-0
                table.table.table-hover.board-table.box-shadow.br-10
                    thead
                        tr
                            th.text-center 번호
                            th.text-center.col-3 제목
                            th.text-center.col-3 작성자
                            th.text-center.col-3.btrr 마감일
                            th.text-center.col-3.btrr 제출여부
                    tbody
                        tr(*ngIf="!items")
                            td.p-6.text-center(colspan=5) 할당된 과제가 없습니다.
                    
                        ng-container(*ngIf="items")
                            tr(*ngFor="let item of items;let i = index")
                                td.text-center {{ item.id }}
                                td
                                    a.text-navy(style="color:#2D8BF8;", (click)="saveTitle(item.title)") {{item.title.length == 0 ? "제목없음" : item.title}}
                                td.text-center.col-3 admin
                                td.text-center.col-3 {{ duedates }}
                                td.text-center.col-3(*ngIf="!files", style="color:red;") 제출안함
                                td.text-center.col-3(*ngIf="files", style="color:blue;") 제출완료